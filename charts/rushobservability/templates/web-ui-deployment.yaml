apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rush.fullname" . }}-web-ui
  labels:
    app.kubernetes.io/component: web-ui
    {{- include "rush.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.webUi.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/component: web-ui
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/component: web-ui
        app.kubernetes.io/instance: {{ .Release.Name }}
      annotations:
        checksum/nginx-config: {{ include (print $.Template.BasePath "/web-ui-nginx-configmap.yaml") . | sha256sum }}
    spec:
      containers:
        - name: web-ui
          image: "{{ .Values.webUi.image.repository }}:{{ .Values.webUi.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.webUi.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
          env:
            - name: QUERY_API_HOST
              value: "{{ include "rush.fullname" . }}-query-api:{{ .Values.queryApi.service.port }}"
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/templates/default.conf.template
              subPath: default.conf.template
          resources:
            {{- toYaml .Values.webUi.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: nginx-config
          configMap:
            name: {{ include "rush.fullname" . }}-web-ui-nginx
