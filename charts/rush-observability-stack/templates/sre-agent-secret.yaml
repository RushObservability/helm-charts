{{- if and .Values.global.sreAgent.enabled .Values.global.sreAgent.llmApiKey (not .Values.global.sreAgent.llmApiKeySecret.name) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "rush.fullname" . }}-sre-agent-llm
  labels:
    app.kubernetes.io/component: sre-agent
    {{- include "rush.labels" . | nindent 4 }}
type: Opaque
data:
  api-key: {{ .Values.global.sreAgent.llmApiKey | b64enc }}
{{- end }}
