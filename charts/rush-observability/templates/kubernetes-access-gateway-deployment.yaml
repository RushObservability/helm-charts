{{- if .Values.enterprise.kubernetesAccess.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rush.componentName" (dict "root" . "component" "kubernetes-access-gateway") }}
  labels:
    {{- include "rush.componentLabels" (dict "root" . "component" "kubernetes-access-gateway") | nindent 4 }}
spec:
  replicas: {{ .Values.enterprise.kubernetesAccess.replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      {{- include "rush.selectorLabels" (dict "root" . "component" "kubernetes-access-gateway") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "rush.selectorLabels" (dict "root" . "component" "kubernetes-access-gateway") | nindent 8 }}
        {{- with (include "rush.podLabels" (dict "root" $ "workload" $.Values.enterprise.kubernetesAccess)) }}
        {{- . | nindent 8 }}
        {{- end }}
      annotations:
        {{- with (include "rush.podAnnotations" (dict "root" $ "workload" $.Values.enterprise.kubernetesAccess)) }}
        {{- . | nindent 8 }}
        {{- end }}
    spec:
      {{- include "rush.workloadScheduling" (dict "root" . "workload" .Values.enterprise.kubernetesAccess) | nindent 6 }}
      {{- include "rush.podSpecOptions" (dict "root" . "workload" .Values.enterprise.kubernetesAccess) | nindent 6 }}
      serviceAccountName: {{ include "rush.serviceAccountName" (dict "root" . "workload" .Values.enterprise.kubernetesAccess "component" "kubernetes-access-gateway") }}
      automountServiceAccountToken: true
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: gateway
          image: {{ include "rush.image" (dict "root" . "image" .Values.enterprise.kubernetesAccess.image "defaultTag" .Chart.AppVersion) | quote }}
          imagePullPolicy: {{ .Values.enterprise.kubernetesAccess.image.pullPolicy }}
          ports:
            - name: https
              containerPort: {{ .Values.enterprise.kubernetesAccess.port }}
              protocol: TCP
          env:
            - name: LISTEN_ADDRESS
              value: {{ printf ":%v" .Values.enterprise.kubernetesAccess.port | quote }}
            - name: KUBE_UPSTREAM_URL
              value: {{ .Values.enterprise.kubernetesAccess.upstream.url | quote }}
            - name: KUBE_UPSTREAM_BEARER_TOKEN_FILE
              value: /var/run/secrets/kubernetes.io/serviceaccount/token
            - name: KUBE_UPSTREAM_CA_FILE
              value: {{ .Values.enterprise.kubernetesAccess.upstream.caFile | quote }}
            - name: KUBE_UPSTREAM_INSECURE
              value: {{ .Values.enterprise.kubernetesAccess.upstream.insecure | quote }}
            - name: RUSH_QUERY_API_URL
              value: {{ .Values.enterprise.kubernetesAccess.queryApiUrl | default (printf "http://%s:%v" (include "rush.queryApiServiceName" .) .Values.queryApi.service.port) | quote }}
            - name: RUSH_GATEWAY_INTERNAL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.queryApi.config.secrets.existingSecret | default (printf "%s-bootstrap" (include "rush.fullname" .)) }}
                  key: kubernetes-access-internal-token
            - name: RUSH_CLUSTER_ID
              value: {{ .Values.enterprise.kubernetesAccess.clusterId | quote }}
            - name: RUSH_GATEWAY_ID
              value: {{ .Values.enterprise.kubernetesAccess.gatewayId | quote }}
            - name: RUSH_GATEWAY_MANAGE_RBAC
              value: {{ .Values.enterprise.kubernetesAccess.rbac.manageRoles | quote }}
            - name: RUSH_GATEWAY_TRUSTED_PROXY_CIDRS
              value: {{ join "," .Values.enterprise.kubernetesAccess.trustedProxyCidrs | quote }}
            - name: RUSH_GATEWAY_RETAIN_RAW_IP
              value: {{ .Values.enterprise.kubernetesAccess.retainRawIp | quote }}
            - name: RUSH_GATEWAY_FAILURE_POLICY
              value: {{ .Values.enterprise.kubernetesAccess.failurePolicy | quote }}
            - name: RUSH_GATEWAY_MAX_RESULT_BYTES
              value: {{ .Values.enterprise.kubernetesAccess.maxResultBytes | quote }}
            - name: RUSH_GATEWAY_MAX_SESSION_BYTES
              value: {{ .Values.enterprise.kubernetesAccess.maxSessionBytes | quote }}
            {{- if .Values.enterprise.kubernetesAccess.tls.existingSecret }}
            - name: TLS_CERT_FILE
              value: /etc/rush-gateway/tls/tls.crt
            - name: TLS_KEY_FILE
              value: /etc/rush-gateway/tls/tls.key
            {{- end }}
          {{- include "rush.extraEnvFrom" (dict "root" . "workload" .Values.enterprise.kubernetesAccess) | nindent 10 }}
          readinessProbe:
            httpGet:
              path: /readyz
              port: https
              scheme: {{ ternary "HTTPS" "HTTP" (not (empty .Values.enterprise.kubernetesAccess.tls.existingSecret)) }}
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: https
              scheme: {{ ternary "HTTPS" "HTTP" (not (empty .Values.enterprise.kubernetesAccess.tls.existingSecret)) }}
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          {{- if or .Values.enterprise.kubernetesAccess.tls.existingSecret .Values.enterprise.kubernetesAccess.extraVolumeMounts .Values.global.extraVolumeMounts }}
          volumeMounts:
            {{- if .Values.enterprise.kubernetesAccess.tls.existingSecret }}
            - name: gateway-tls
              mountPath: /etc/rush-gateway/tls
              readOnly: true
            {{- end }}
            {{- with (include "rush.extraVolumeMounts" (dict "root" $ "workload" $.Values.enterprise.kubernetesAccess)) }}
            {{- . | nindent 12 }}
            {{- end }}
          {{- end }}
          resources:
            {{- toYaml .Values.enterprise.kubernetesAccess.resources | nindent 12 }}
          securityContext:
            {{- include "rush.hardenedContainerSecurityContext" . | nindent 12 }}
      {{- if or .Values.enterprise.kubernetesAccess.tls.existingSecret .Values.enterprise.kubernetesAccess.extraVolumes .Values.global.extraVolumes }}
      volumes:
        {{- if .Values.enterprise.kubernetesAccess.tls.existingSecret }}
        - name: gateway-tls
          secret:
            secretName: {{ .Values.enterprise.kubernetesAccess.tls.existingSecret }}
        {{- end }}
        {{- with (include "rush.extraVolumes" (dict "root" $ "workload" $.Values.enterprise.kubernetesAccess)) }}
        {{- . | nindent 8 }}
        {{- end }}
      {{- end }}
{{- end }}
